#include <stdlib.h>
#include "moteur.h"

void shift_shape_left(Liste *lst, Token *tok) {
    /* Liste vide ou token vide */
    if (*lst == NULL || tok == NULL) {
        return;
    }
    /* Token seul de sa forme */
    if (tok->next_shape = tok) {
        return;
    }
}